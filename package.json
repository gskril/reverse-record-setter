{
  "name": "reverse-record-setter",
  "private": true,
  "workspaces": [
    "packages/*"
  ],
  "scripts": {
    "dev": "bun run --filter '*' dev",
    "dev:api": "bun run --filter api dev",
    "dev:frontend": "bun run --filter frontend dev",
    "build": "bun run --filter '*' build",
    "build:frontend": "bun run --filter frontend build"
  },
  "devDependencies": {
    "@types/bun": "1.3.6",
    "typescript": "5.9.3"
  }
}
