{
  "name": "ens-reverse-records",
  "private": true,
  "workspaces": {
    "packages": [
      "packages/*"
    ],
    "catalog": {
      "typescript": "5.9.3",
      "@types/bun": "1.3.6",
      "zod": "4.3.6",
      "viem": "2.44.4",
      "wrangler": "4.60.0"
    }
  },
  "scripts": {
    "dev": "bun run --filter '*' dev",
    "dev:api": "bun run --filter api dev",
    "dev:frontend": "bun run --filter frontend dev",
    "build": "bun run --filter '*' build",
    "format": "prettier --write ."
  },
  "devDependencies": {
    "@trivago/prettier-plugin-sort-imports": "6.0.2",
    "@types/bun": "catalog:",
    "prettier": "3.8.1",
    "typescript": "catalog:"
  }
}
